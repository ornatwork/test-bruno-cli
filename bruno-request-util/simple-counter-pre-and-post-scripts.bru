meta {
  name: simple-counter-pre-and-post-scripts
  type: http
  seq: 1
  tags: [
    util
  ]
}

get {
  url: https://docs.usebruno.com/
  body: none
  auth: inherit
}

script:pre-request {
  const util = require('@paystra/bruno-util').util;
  
  // Update unique global counter ( counter )
  util.updateCounter();
  
  
}

script:post-response {
  const util = require('@paystra/bruno-util').util;
  
  
}
